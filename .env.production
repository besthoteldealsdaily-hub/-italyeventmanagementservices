# Loaded automatically by `next build` in production, regardless of the Cloudflare
# dashboard's Build Variables setting — this stops indexing being blocked by a
# dashboard variable that didn't get saved / picked up by the build.
# Safe to commit: NEXT_PUBLIC_* values are public, they ship in the client bundle anyway.
NEXT_PUBLIC_ALLOW_INDEXING=true
